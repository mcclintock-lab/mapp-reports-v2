

<div class="reportSection">
  <h4>Size</h4>
  <p class="large">
    This zone is <strong>{{size}} square kilometers</strong>, which represents <strong>{{percent}}%</strong> of the {{sc_name}}. Size is used to quantify draft spatial zones and provide percent coverage. 
  </p>
</div>

<div class="reportSection">
  <h4>Coastline Length</h4>
  <p class="large">
    This sketch includes <strong>{{coastlineLength}} kilometers</strong> of coastline.
  </p>
</div>

<div class="reportSection">
  <h4>Average Depth</h4>
   <p class="large">
    The average depth of this sketch is <strong>{{avgDepth}} meters</strong> below sea level. The 
    maximum depth is <strong>{{minDepth}}</strong> meters below sea level, and the minimum depth is <strong>{{maxDepth}}</strong> meters below sea level.
  </p>
</div>

{{#anyAttributes}}
<div class="reportSection">
  <h4>{{sketchClass.name}} Attributes</h4>
    {{>attributes/attributesTable}}
  </table>
</div>
{{/anyAttributes}}

<div class="reportSection tableContainer">
  <h4>Sketch Name and Id</h4>
    <table>
      <thead>
      <tr>
        <th style="width:200px;">Sketch Name </th>
        <th>Sketch ID</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>{{sketch_name}}</td>
        <td>{{sketch.id}}</td>
      </tr>
    </tbody>
    </table>
</div>